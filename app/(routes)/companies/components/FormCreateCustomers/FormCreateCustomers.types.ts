// Utilizo para verificar la creacion del modal exitoso

import { Dispatch, SetStateAction } from "react";

export type FormCreateCustomerProps = {
  setOpenModalCreate?: Dispatch<SetStateAction<boolean>>;
};
